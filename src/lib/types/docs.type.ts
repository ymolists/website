export type comparisonItem = {
  title: string;
  mobileTitle: string;
  value: number;
  slotName: string;
  hidden?: boolean;
};

export type AvailabilityItem = {
  label: string;
  isSaas: boolean;
};
