export type GetStartedIcon = {
  path: string;
  transform: string;
};

export type GetStartedItem = {
  title: string;
  description: string;
  buttonLabel: string;
  path: string;
  icon: GetStartedIcon;
};
