@tailwind base;
@tailwind components;
@tailwind utilities;

*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  box-sizing: border-box;
  scroll-behavior: smooth;
}

:root {
  --font: "ABCDiatype", sans-serif;

  --black: #12100c;
  --dark-grey: #12100cb3;
  /* --dark-grey: #565451; */
  --light-grey: #999795;
  --brand-almost-ripe: #ffb45b;
  --brand-ripe: #ff8a00;
  --brand-gradient: linear-gradient(137.41deg, #ffad33 14.37%, #ff8a00 91.32%);
  --brand-light: #ffe4bc;
  --salmon: #ffb0ae;
  --divider: #dadada;
  --sand-dark: #ece7e5;
  --sand-light: #f3f1f0;
  --off-white: #f9f9f9;
  --white: #fff;

  --h1: 5rem;
  --h2: 3.375rem;
  --h3: 3rem;
  --h4: 2rem;
  --h5: 1.75rem;
  --h6: 1.25rem;
  --p-large: 1.5rem;
  --p-medium: 1.125rem;
  --p-small: 1rem;
  --btn-small: 0.875rem;
  --fine-print: 0.75rem;

  --header-height: 5rem;
  --row-max-width: 83.75rem;
  --xx-large: 10rem;
  --x-large: 4.5rem;
  --large: 4rem;
  --medium: 3.5rem;
  --small: 3rem;
  --x-small: 2rem;
  --xx-small: 1.5rem;
  --micro: 1rem; /* should we invert micro and macro? */
  --macro: 0.5rem;

  --shadow: 0rem 1rem 2.4rem 0rem rgba(0, 0, 0, 0.08);

  --border-light: 1px solid var(--sand-dark);

  @media (max-width: 1140px) {
    --h1: 3rem;
    --h2: 2.5rem;
    --h3: 2rem;
    --h4: 1.5rem;
    --h5: 1.25rem;
    --h6: 1.123rem;
    --p-large: 1.125rem;
    --p-medium: 1rem;
    --p-small: 0.875rem;
    --btn-small: 0.75rem;
    --fine-print: 0.75rem;

    --small: 2.5rem;
  }
}

body {
  font-family: var(--font);
  font-size: var(--p-medium);
  color: var(--dark-grey);
  background: var(--sand-light);
  overflow-x: hidden;
  min-height: 100vh;
  text-rendering: optimizeSpeed;
  line-height: 1.5;
}

/* Remove default margin */
body,
h1,
h2,
h3,
h4,
p,
figure,
blockquote,
dl,
dd {
  margin: 0;
}

/* Remove margin, padding and list styles on ul, ol elements with a class attribute (leave content lists alone. may need to revisit this */
ul[class],
ol[class] {
  margin: 0;
  padding: 0;
  list-style: none;
}


/* Make images easier to work with */
img,
picture {
  max-width: 100%;
  display: block;
  object-fit: contain;
}

/* Ensure accessibility. Remove outline if we may miss images in prod */
img:not([alt]) {
  filter: blur(10px);
  outline: 10px solid;
}

/* margin on everything except first child.  */
main > * + * {
  margin-top: 1em;
}

/* Inherit fonts for inputs and buttons */
input,
button,
textarea,
select {
  font: inherit;
}

/* Make site accessible for those who request it, no matter what! */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* -------------------------------------- */
/* --- Layout Helpers --- */
/* -------------------------------------- */

.row {
  width: 90%;
  margin: 0 auto;
  max-width: var(--row-max-width);

  @media (max-width: 1032px) {
    width: 94%;
  }

  @media (min-width: 1141px) {
    width: 92%;
  }

  @media (max-width: 768px) {
    width: 100%;
    padding: 0 var(--micro);
  }

  @media (max-width: 375px) {
    padding: var(--macro);
  }
}

/* -------------------------------------- */
/* --- Buttons & Links --- */
/* -------------------------------------- */

a {
  color: var(--black);
  transition: all 0.2s 50ms;
}

.link--light-grey {
  font-weight: 600;
  color: var(--light-grey);
}

.btn,
[class|="btn"] {
  display: inline-block;
  font-weight: 600;
  padding: 0.5em 1.5em;
  border-radius: 1em;
  box-shadow: 0px 1px 1px rgba(0, 0, 0, 0.1);
  background-color: var(--sand-dark);
  font-size: var(--btn-small);
  &:hover {
    background-color: var(--brand-almost-ripe);
  }
  &.btn-conversion {
    padding: 0.75em 2em;
    font-size: var(--p-large);
    background-color: var(--brand-almost-ripe);
  }
  &.btn-primary {
    background-color: var(--brand-almost-ripe);
  }
  &.btn-secondary {
    background-color: var(--salmon);
  }
  &.btn-tertiary {
    background-color: var(--black);
    color: var(--white);
  } 
  &.btn-quaternary {
    background-color: var(--white);
  }
}
.btn-conversion,
.btn-primary,
.btn-secondary,
.btn-tertiary {
  &:hover {
    background-image: linear-gradient(rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.1));
  }
}


.btn-wrapper {
  --gap: var(--micro);
  display: flex;
  flex-wrap: wrap;
  margin: calc(-1 * var(--gap)) 0 0 calc(-1 * var(--gap));
  width: calc(100% + var(--gap));
  @media (max-width: ${sizes.breakpoints.sm}) { /* TW */
    justify-content: center;
  }
  & > * {
    margin: var(--gap) 0 0 var(--gap);
  }
}

.btn--brand {
  background: var(--brand-almost-ripe);
  padding: 0.75rem var(--x-small);

  @media (min-width: 1141px) {
    min-width: 10rem;
  }

  @media (max-width: 868px) {
    padding: 0.75rem var(--xx-small);
  }
}

.btn--small {
  font-size: var(--btn-small);
  padding: var(--macro) var(--x-small);
}

// we may want to come up with purpose names instead of color names. like primary / secondary / etc.
.btn--salmon {
  background: var(--salmon);
}

.btn--almost-ripe {
  background: var(--brand-almost-ripe);
}

.btn--dark {
  color: var(--off-white);
  background: var(--black);
}

/* -------------------------------------- */
/* --- Typography --- */
/* -------------------------------------- */

h1,
h2,
h3,
h4,
h5,
h6 {
  font-weight: 600; /* this is default. it's removed in TW. Should we just remove it from the TW reset? */
}

h1, .h1 {
  font-size: var(--h1);
  line-height: 110%;
}

h2, .h2 {
  margin-bottom: var(--small);
  font-size: var(--h2);
  line-height: 118.5%;

  @media (max-width: 375px) {
    margin-bottom: var(--x-small);
  }
}

h3, .h3 {
  margin-bottom: var(--macro);
  font-size: var(--h3);
  line-height: 133.33%;
}

h4, .h4 {
  margin-bottom: var(--macro);
  font-size: var(--h4);
  line-height: 125%;
}

h5, .h5 {
  font-size: var(--h5);
  margin-bottom: var(--macro);
  line-height: 143%;
}

h6, .h6 {
  font-size: var(--h6);
  line-height: 120%;
}

.text-large {
  font-size: var(--p-large);
  line-height: 133.33%;
}

.text-medium {
  font-size: var(--p-medium);
  line-height: 177.77%;
}

.text-small {
  font-size: var(--p-small);
  line-height: 150%;
}

.btn-small {
  font-size: var(--btn-small);
  line-height: 171%;
}

.fine-print {
  font-size: var(--fine-print);
  line-height: 133.33%;
}

@media (max-width: 375px) {
  .text-large {
    font-size: var(--p-medium);
  }
}

/* ---------------------------- */
/*        Site Styles           */
/* ---------------------------- */

.card {
    padding: var(--medium);
    background: var(--white);
    border-radius: 1.25rem;
}


